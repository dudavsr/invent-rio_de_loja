#include <iostream>
#include <vector>
#include <list>
#include <set>
#include <map>
#include <string>
using namespace std;

class Produto {
    string nome;
    float preco;
    int estoque;

    //construtor
    Produto(string n, float p, int e) : nome(n), preco(p), estoque(e) {}
};

int main() {
    
    vector<Produto> produtos;

}